{
  "permissions": {
    "allow": [
      "Bash(gh repo *)",
      "Bash(gh issue create --repo 3000-Labs/latch-mobile --title 'Backup upload failure during onboarding is silent and unrecovered' --body ' *)",
      "Bash(gh issue create --repo 3000-Labs/latch-mobile --title 'POST /v1/backup payload shape doesn'\\\\''t match backend contract — every backup upload fails' --body ' *)",
      "Bash(go install *)",
      "Bash(git add *)",
      "Bash(gofmt -w internal/config/config.go)",
      "Bash(gofmt -l internal/config/config.go)",
      "Bash(go build *)",
      "Bash(go test *)",
      "Bash(git rebase *)",
      "Bash(gh pr create --repo 3000-Labs/latch-api --base master --head feat/relayer-memo-registration --title 'Register smart accounts with latch-relayer for pooled-deposit memo routing' --body ' *)"
    ]
  }
}
