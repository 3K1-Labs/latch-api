// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: cleanup.sql

package db

import (
	"context"
	"time"
)

const deleteExpiredCosignRequests = `-- name: DeleteExpiredCosignRequests :execrows

DELETE FROM cosign_requests
WHERE expires_at < $1
`

// Retention / garbage collection. Background sweeps that bound table growth.
// The cosign queue is high-churn (every multisig tx creates a request with a
// ~23h TTL); without this it grows unbounded. wck_bundles and wallet_memberships
// are long-lived discovery/bootstrap state and are only swept on a far horizon.
// Every request carries expires_at (~23h from creation) regardless of status,
// so one cutoff reaps expired-pending, submitted, and cancelled rows alike.
// cosign_signatures rows are removed by ON DELETE CASCADE.
func (q *Queries) DeleteExpiredCosignRequests(ctx context.Context, before time.Time) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteExpiredCosignRequests, before)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const deleteStaleWCKBundles = `-- name: DeleteStaleWCKBundles :execrows
DELETE FROM wck_bundles
WHERE updated_at < $1
`

func (q *Queries) DeleteStaleWCKBundles(ctx context.Context, before time.Time) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteStaleWCKBundles, before)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const deleteStaleWalletMemberships = `-- name: DeleteStaleWalletMemberships :execrows
DELETE FROM wallet_memberships
WHERE created_at < $1
`

func (q *Queries) DeleteStaleWalletMemberships(ctx context.Context, before time.Time) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteStaleWalletMemberships, before)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
